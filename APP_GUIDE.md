# AI Literature Organizer｜應用程式介紹與操作流程

AI Literature Organizer 是 Windows 桌面文獻整理工具。它讀取 PDF 文字，使用 Google Gemini 依研究者自訂的分類與標籤分析文獻，建立 Excel 索引，並保留原始 PDF 備份。

## 主要功能

- 自訂研究主題、主要分類、標籤與同義詞；每篇文獻選一個主要分類及最多七個標籤。
- 選取代表文獻，讓 AI 提出分類與標籤草案；人工審核後才套用。
- 擷取書目資訊，自訂 Excel 欄位、欄位標題與整理版 PDF 檔名。
- 輸出 CSV、Excel、整理版 PDF 和原始備份；在程式內搜尋索引並開啟 PDF。
- 檢查重複檔案，保留失敗檔案與執行紀錄，支援程式更新。

## 安裝與準備

若 [GitHub Releases](https://github.com/mercury12100/literature-organizer-releases/releases) 已提供 `Literature_Organizer_Setup.exe`，下載後依畫面安裝。是否有可下載的安裝檔，以 Releases 頁面為準。

從原始碼啟動時，請在 Windows 64 位元環境安裝 Python 3.10～3.13，完整解壓縮原始碼，依序執行 `01_Setup.bat` 和 `02_Start.bat`。

首次使用還需要 [Google AI Studio API Key](https://aistudio.google.com/apikey)。API Key 不會存入程式設定檔。PDF 擷取的文字會傳送至 Gemini 分析；請先確認文獻內容適合傳送到該服務。API 用量及費用依個人 Google 帳戶設定。

## 第一次整理：操作流程

1. **建立分類設定**：切到「分類與標籤」，填入模板名稱及研究說明。每行輸入一個主要分類或標籤；至少填其中一種。可按「選代表文獻，產生／補充分類與標籤」，挑選 1～10 篇樣本，審核建議後按「儲存模板」。新建議的標籤名稱沿用文獻原文。不要的自訂模板可按「刪除目前模板」；內建空白模板會保留。
2. **設定輸出**：如需調整 Excel 欄位、標題或 PDF 檔名，到「輸出與更新」按「設定 Excel 欄位、標題與檔名」。其中可勾選「國家」與「研究區域」；它們只擷取研究地點，不使用作者機構地址，缺乏原文佐證時會留空。自行新增欄位也須填具體擷取說明。再回「分類與標籤」儲存模板。
3. **選擇來源與模型**：到「批次整理」，輸入 Gemini API Key，按「讀取可用模型」並選擇模型。選取「PDF 資料夾」及另一個「輸出資料夾」；需要時勾選包含子資料夾，並確認研究模板。
4. **開始整理**：按「開始整理」，從進度列、摘要與紀錄查看結果。「停止」會等待目前的 API 請求回應或逾時；已完成的文獻仍保留。
5. **檢查結果**：到「文獻索引」搜尋標題、作者、分類或標籤，雙擊文獻開啟整理版 PDF。按「開啟輸出資料夾」查看檔案，並抽查 AI 判讀及 Excel 內容。

## 輸出內容

| 路徑 | 內容 |
| --- | --- |
| `Papers/` | 依設定命名、寫入中繼資料的整理版 PDF |
| `Originals/` | 原始 PDF 備份 |
| `Duplicates/` | 判定為同篇文獻的額外 PDF 備份 |
| `Failed/` | 處理失敗檔案的副本；來源檔保留 |
| `Logs/` | 執行紀錄與本次分類設定 |
| `literature_data.csv` | 累積文獻資料 |
| `literature_index.xlsx` | Excel 文獻索引 |

成功整理或確認已有完整重複副本後，程式會驗證備份並移除來源 PDF；失敗時保留來源檔。要重試失敗檔，可選 `Failed` 作為來源資料夾。建議首次使用先以少量 PDF 試跑。

## 後續操作

- **修改分類或標籤**：到「分類與標籤」修改並儲存模板。自動建議的新名稱沿用文獻原文；可用新樣本文獻補充分類與細部標籤。新設定只用於往後的整理，不會自動重跑舊文獻。若要請研究員討論分類設計，可按「向 ChatGPT 詢問標籤建議」：程式只複製詢問內容並開啟網站，由研究員自行貼上送出。
- **修改 Excel 欄位或標題**：到「輸出與更新」調整後按「重建 Excel」。此操作不呼叫 AI，也不會替舊文獻補擷取新欄位或重新命名 PDF。Excel 的分類說明會要求 AI 用繁體中文撰寫；既有資料中的 `Other`、`taxonomy` 會以「未分類」、「分類設定」顯示。
- **英文檔名**：新產生的 PDF 檔名只使用英文字母、數字及常見 ASCII 符號。中文主要分類若有英文同義詞會優先使用；沒有可用英文名稱的欄位以 `Unknown` 代替。
- **查詢中文術語**：分類說明保留學術名詞原文。需要中文譯名時，在「分類與標籤」按「網路查證中文術語」，使用支援 Google 搜尋的 Gemini 模型查詢；沒有可核對來源時不採用譯名。此功能需要 API Key，可能產生搜尋用量。
- **意見回饋**：在「輸出與更新」按「意見回饋」，填寫主旨和內容，程式會開啟預填的 GitHub Issues 頁面。請在瀏覽器確認並提交；開啟頁面本身不代表意見已送出。
- **更新程式**：到「輸出與更新」按「檢查網路更新」，依畫面下載及套用；也可匯入信任來源的相容更新 ZIP。
- **處理掃描型 PDF**：若 PDF 沒有足夠可擷取文字，請先以 OCR 轉成可搜尋 PDF 再匯入。

原始碼安裝、打包及發布細節請參閱 `README.md` 與 `PUBLISH_GUIDE.md`。
